# Copy to .env.local and fill in your key from https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional — defaults to gemini-2.5-flash
# GEMINI_MODEL=gemini-2.5-flash

# Max in-flight Gemini API calls (default 1). Raise to 2 after confirming stability.
GEMINI_MAX_CONCURRENT=1

# Retries on transient 429/503 (default 3)
GEMINI_MAX_RETRIES=3
